# Results

`CURRENT` contains the release identifier used by documentation and validation.
Immutable evidence for a release lives under `frozen/<release-id>/`. Recomputed
local outputs go to `audit/` and are ignored by Git unless deliberately promoted
through the freezing protocol in `docs/RESULT_FREEZING.md`.
