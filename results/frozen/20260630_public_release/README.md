# Frozen release: 20260630_public_release

This release binds the public preprint dated 2026-06-30 to the aligned BTC-hourly
return matrix identified in `data/manifest.json`. `statistics.json` is generated
by `experiments/run_statistics.py`; `checksums.sha256` binds the frozen files.
Changing an artifact requires a new release identifier rather than editing this
directory in place.
