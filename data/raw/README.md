# Raw data policy

Raw data are intentionally not included. The experiments used public Binance
spot/futures market-data endpoints for crypto assets and Yahoo Finance `GC=F`
for gold. Source availability and provider terms can change; users should record
download time, endpoint, symbol, interval, timezone, and checksum when rebuilding
the inputs. The frozen statistical claims depend on the released processed
matrix, not on a future re-download.
