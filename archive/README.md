# Historical workspace

`legacy-source/` preserves the complete pre-restructure OneDrive workspace,
including work orders, exploratory tracks, internal reports, source documents,
and submission artifacts. It is excluded from Git because it mixes historical,
redundant, internal, and potentially non-release material. Nothing in that
directory is a public evidence source unless explicitly promoted into the
versioned repository with provenance and a checksum.
