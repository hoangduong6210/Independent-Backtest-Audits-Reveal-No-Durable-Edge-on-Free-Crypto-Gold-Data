# Figures

`generated/` contains the disclosure-safe figures used by the current Markdown
manuscript. Regenerate them with `python scripts/generate_current_paper_figures.py`.
The script fixes its random seed. Several figures are explanatory reconstructions
rather than raw empirical plots; captions in the manuscript state their role.
